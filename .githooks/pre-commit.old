#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"
"$ROOT_DIR/scripts/guard-upstream-remote.sh" enforce

GIT_DIR="$(git rev-parse --git-dir)"
LEGACY_HOOK="$GIT_DIR/hooks/pre-commit"
if [[ -x "$LEGACY_HOOK" ]]; then
  exec "$LEGACY_HOOK" "$@"
fi
